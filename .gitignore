# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/

# Env
.env
!.env.example

# Загружаемые пользователями файлы (аватары) — только на volume, не в репозиторий
backend/media/

# Живые креды Postbox — в репозиторий не попадают
docs/smtp.md

# Node / Expo
node_modules/
.expo/
web/.next/

# Веб-бандл PWA: выкладывается на ВМ (docs/deploy.md, шаг 4b), не в репозиторий
infra/web/*
!infra/web/.gitkeep

# IDE / OS
.idea/
.DS_Store

# Claude Code local (агенты проекта — общие, версионируем)
.claude/*
!.claude/agents/
!.claude/skills/
!.claude/commands/
# локальные настройки Claude в подпроектах
*/.claude/
